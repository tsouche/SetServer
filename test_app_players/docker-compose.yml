version: '3'
volumes:
  set-data:
networks:
  set-net:
services:
  set-db:
    image: mongo:4.0.1
    volumes:
      - set-data
    ports:
      - "27017:27017"
    networks:
      - set-net
  set-app:
    build: ./app
    ports:
      - "8080:8080"
    networks:
      - set-net
